import { NumberLineViewModel } from "number-line";

export function render(viewModel:NumberLineViewModel,container:HTMLElement) {
	const div = document.createElement("div");
	div.innerHTML = "html";
	return div;
}